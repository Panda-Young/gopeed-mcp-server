# Gopeed API 地址。
# 推荐留空端口（http://127.0.0.1/api/v1），server 会自动发现 Gopeed 当前端口。
# Gopeed 每次重启都会随机更换端口，自动发现可免去手动改配置。
# 仅在确需固定端口时才写成 http://127.0.0.1:<端口>/api/v1
GOPEED_API_URL=http://127.0.0.1/api/v1

# Gopeed API 令牌（可选，仅当 Gopeed 配置了访问令牌时填写）
# GOPEED_API_TOKEN=your_token_here

# HTTP 请求超时时间（秒，默认 10）
# GOPEED_TIMEOUT=10
